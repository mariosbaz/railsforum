
	<h2 id="header">User Name: <%="#{@user.email.split('@')[0]}"%></h2>   

<% if @user.topics.any?%>
	<p id= "label">Here is a list of the Topics I've created</p>
	    </div>
<div class="container" id="users_container">
 <div class="row" id="user_header">

    <div class="span4">Topic Name</div>
    <div class="span4">Created At</div>
    <div class="span4">Actions</div>    
</div> 
  <div class="row">
    <div class="span12">
 <% @user.topics.each do |topic| %>        
 <div class="row" id="user_box">
            <div class="span4 "><%= topic.name %></div>
            <div class="span4"><%=time_ago_in_words(topic.created_at)%> ago
            </div>
            <div class="span4"><%=link_to "Show", topic_path(topic), :class => 'btn' %>
            <%if user_signed_in? and current_user.id==topic.user_id  %>
            <%=link_to "Delete",topic , method: :delete, confirm: "Are You sure?", :class => "btn btn-default btn-lg"%>
            <%=link_to "Edit",edit_topic_path(topic), :class => 'btn' %>
            <%end%></div>        
        </div>
       <%end%>
    </div>   
    </div>
 
</div>  
</div>
			

	</div>
	<%else%>
	<%end%>


<div class="container">
<%= link_to "Back", :back, class: "btn btn-large btn-primary" %>
<%= link_to "New Topic", new_topic_path, class: "btn btn-large btn-primary" %>
</div>